# Portfolio Status Tracker

Tracks exactly what's been done on each project, so nothing gets redone or skipped across sessions. Update this file every time a stage is completed on any project.

**Stages, in order:**
1. **Review-Fix** — full file-by-file review, bugs/security/broken-feature fixes, dashboard check
2. **Deep Verify** — every statistic independently recomputed from raw data + every citation/reference checked against a real source
3. **Doc Hygiene** — index/TOC only where genuinely warranted, heading/numbering consistency across Research Paper / Project Journal / Development Log
4. **External AI Review** — user gets outside AI review(s), findings triaged and incorporated
5. **CITATION.cff + LICENSE** — added to repo
6. **Zenodo Published** — GitHub release cut, DOI minted, badge added to README + dashboard + CITATION.cff
7. **Dashboard Screenshots** — process/workflow screenshots added inside the dashboard (QGIS + VS Code for all projects; also GEE for projects where Google Earth Engine was used)

| Project | 1. Review-Fix | 2. Deep Verify | 3. Doc Hygiene | 4. External AI Review | 5. CITATION+LICENSE | 6. Zenodo DOI | 7. Screenshots |
|---|---|---|---|---|---|---|---|
| GPIE (`GREEN_POLICY_INTELLIGENCE_SYSTEM`) | ✅ | ✅ full — every reported statistic in Research_Paper.md independently recomputed by re-running the project's own causal-inference scripts against `data/master_dataset.csv`/`master_dataset_control.csv`, plus hand-written reimplementations of the 4 robustness checks + MDE calc that have no saved script; 3 of 13 references spot-checked (all real); **1 real inconsistency found and fixed** — see fix log below | ✅ clickable Index (50 entries, incl. nested sub-items for the Sentinel-5P protocol doc) added to Devlopment_Log.md 2026-08-03 | ✅ (earlier round) | ✅ | ✅ `10.5281/zenodo.21756661` (v1.0.1) — large dashboard DOI badge added 2026-08-03 | ⬜ not done — GEE used? TBD, confirm before this stage |
| DOUBLE_JEOPARDY | ✅ | ✅ full — all independently re-derivable statistics recomputed by re-running the project's own scripts against raw/processed data (mangrove 3-point areas, coral DHW period-comparison + Mann-Kendall trend p-values, compound vulnerability scores, WDPA area/ratio, governance Pearson r + Fisher's z CI); §4.1 physical-exposure % explicitly flagged as not independently re-derivable (requires multi-GB DEM/population rasters) but code-reviewed for logic; 3 of 8 references spot-checked (all real); **1 real inconsistency found and fixed** — see fix log below | ✅ clickable Index (20 entries) added to Devlopment_Log.md 2026-08-03 | ⬜ not done | ✅ | ✅ `10.5281/zenodo.21739961` — large dashboard DOI badge added 2026-08-03 (dashboard had no badge at all before) | ✅ done — 4 QGIS + 4 VS Code screenshots added to dashboard |
| ECOCIDE | ✅ | ⬜ not yet done | ✅ clickable Index (33 entries) added to Devlopment_Log.md 2026-08-03 | ⬜ not done | ✅ | ✅ `10.5281/zenodo.21757974` — large dashboard DOI badge added 2026-08-03 | ⬜ not done — satellite-based (Sentinel-2/VIIRS), likely GEE used → needs GEE + QGIS + VS Code, confirm with user first |
| BORDER_OPTICS | ✅ | 🟡 partial — key stats (Mann-Whitney, Holm-Bonferroni) + 1 pivotal citation (Lok Sabha Q508) verified, but no full reference-by-reference sweep yet | ✅ | ✅ (3 reviews incorporated) | ✅ | ✅ `10.5281/zenodo.21759970` — large dashboard DOI badge added 2026-08-03 | ⬜ not done — satellite-based (Sentinel-2 NDBI/VIIRS), likely GEE used → needs GEE + QGIS + VS Code, confirm with user first |
| GHOST_INFRASTRUCTURE | ✅ | ✅ full — all stats recomputed from raw `.gpkg` (t=42.887, r=0.063, logit coef exact match), all 6 references checked, 1 unverifiable duplicate removed, Cohen's d + odds ratios independently recomputed and verified exact | ✅ clickable Index added to Devlopment_Log.md (same convention as BORDER_OPTICS); no H1→H3 skips; "Module Architecture" outline's all-caps `# MODULE N` headers still duplicate H1 mid-document rather than `##` — cosmetic, not fixed | ✅ 4 external AI reviews (Deepseek/Claude/Gemini/Chatgpt) triaged — see fix log below | ✅ | ✅ `10.5281/zenodo.21761320` (v1.0.0) — badge added to README, CITATION.cff, dashboard app.py; dashboard badge upgraded to the large card style 2026-08-03 | ⬜ not done — QGIS + VS Code (no GEE — no satellite data used, OSMnx/QGIS only) |
| GREEN_ALIBI | ✅ | ✅ full — every reported statistic recomputed independently from raw/processed data (climatology mean/std, anomalies, Pearson r=0.837/Spearman ρ=0.857, all 15 lag-threshold crossings); all 6 references verified against real sources; **1 major bug found and fixed** — see fix log below | ✅ Index added to Development_Log.md, headings consistent | ✅ 4 external AI reviews (Deepseek/Claude/Gemini/Chatgpt) triaged and acted on — see fix log below | ✅ `CITATION.cff` + `LICENSE` (CC BY 4.0) added, README updated, matching GHOST_INFRASTRUCTURE's format | ✅ `10.5281/zenodo.21762501` (v1.0.0) — badge added to README, CITATION.cff, dashboard app.py; dashboard badge upgraded to the large card style 2026-08-03 | ⬜ not done — GEE confirmed used (NDVI/rainfall/boundary via Earth Engine, per Research_Paper.md §3.1) → needs GEE + QGIS(N/A, no QGIS used) + VS Code, confirm exact toolset with user |
| STOLEN_STRATA | ✅ | ✅ full — every headline statistic independently recomputed from raw rasters/OSM data (all 4 bare-earth-fraction years, 190.3 ha net conversion + 67% concentration, 14/225.4 ha saffron, all 6 proximity-sensitivity points, Mann-Whitney p=0.0116); **1 reproducibility bug found and fixed** — see fix log below | ✅ headings normalized, Hinglish note rewritten, Index added to Devlopment_Log.md | ✅ 4 external AI reviews (Deepseek/Claude/Gemini/Chatgpt) triaged and acted on — see fix log below | ✅ `CITATION.cff` + `LICENSE` (CC BY 4.0) added, README updated, matching GREEN_ALIBI/GHOST_INFRASTRUCTURE format | ✅ `10.5281/zenodo.21766464` (v1.0.0) — badge added to README, CITATION.cff, and dashboard app.py **using a new, larger dashboard badge style** (see backlog note below) | ⬜ not done — GEE used (interactively) + QGIS + VS Code, confirm exact toolset with user |

**Backlog — enlarge the dashboard DOI badge across the portfolio (raised by user, 2026-08-03) — ✅ DONE, all 7 projects (2026-08-03):** the plain `zenodo.org/badge/DOI/....svg` image was too small/thin inside a Streamlit page and could go unnoticed. STOLEN_STRATA's dashboard introduced a new, bigger style instead — a styled, color-bordered card with a 📦 icon, "ARCHIVED & CITABLE ON ZENODO" label, and the DOI in large bold text, clickable through to doi.org (see `dashboard/app.py`, right under the hero title). This same card pattern (background gradient + 2px accent-color border + box-shadow glow, using each project's own actual palette variables) has now been retrofitted into all 6 remaining published projects' dashboard home pages, one at a time:
  - **BORDER_OPTICS** (`app.py` at repo root — the live Streamlit entry point; the `dashboard/pages/` folder present in this repo is unused/stale) — replaced the small badge, using `utils/theme.py`'s `PALETTE` (border: `accent_vintage` watermelon, label: `accent` mint).
  - **GREEN_ALIBI** (`app.py` at repo root) — replaced the small badge, using `utils/style.py`'s `MAGENTA`/`TEAL`/`NAVY_DARK`/`NAVY_MED`/`TEXT_LIGHT`.
  - **GHOST_INFRASTRUCTURE** (`dashboard/app.py`) — replaced the small badge, using `dashboard/styles.py`'s `PALETTE` (border/label: gold `accent` `#FFF2BA`, on the navy `bg_main`/`bg_sidebar` gradient).
  - **DOUBLE_JEOPARDY** (`dashboard/app.py`) — this project had **no DOI badge at all** yet (small or large); added the big-card badge fresh, using `dashboard/styles.py`'s `PALETTE` (border: `navy` cyan `#22D3EE`, label: `green` gold `#FFD60A`).
  - **GPIE** (`dashboard/app.py`) — replaced the small badge, using `dashboard/styles.py`'s `PALETTE` (border: `coral`, label: `chantilly`, text: `text`).
  - **ECOCIDE** (`dashboard/app.py`) — replaced the small badge, using `dashboard/styles.py`'s `PALETTE` (border/label: `accent` teal `#00ACC1`).
  All 6 files delivered and written back to the user's device via the device bridge. README badges were left as the standard small markdown badge everywhere (that's the normal GitHub convention and renders fine there) — this was specifically a dashboard-page sizing fix. This should be the default going forward for any future project's DOI badge.

**Stage 7 note:** DOUBLE_JEOPARDY is the reference pattern (4 QGIS + 4 VS Code screenshots, added inside the dashboard). Every other project needs the same treatment. For any project that used Google Earth Engine in its pipeline, add GEE screenshots too (3 tools total: GEE + QGIS + VS Code) — GEE usage is not yet confirmed per project above and should be checked with the user before doing that project's screenshot round.

**Retroactive plan (per user's instruction):** go back and bring GPIE, DOUBLE_JEOPARDY, ECOCIDE, and BORDER_OPTICS up to the same "Deep Verify" standard now applied to GHOST_INFRASTRUCTURE (stage 2), on top of their existing review-fix work. Doing one project at a time, in that order. **GPIE done (2026-08-03), DOUBLE_JEOPARDY done (2026-08-03) — see fix logs below.** ECOCIDE, BORDER_OPTICS not started yet.

**DOUBLE_JEOPARDY — Deep Verify (2026-08-03):**
- Staged and re-ran the project's own analysis scripts directly against raw/processed data: `coral_trend_test.py` (5 islands' DHW CSVs → Mann-Kendall + OLS), `mangrove_3point_comparison.py` (9 GMW gpkg files, EPSG:6933 area), `compound_vulnerability_score.py` (hardcoded inputs + min-max normalization), `compute_wdpa_area.py` → `normalize_wdpa.py` → `wdpa_coastal_buffer.py` → `governance_correlation_test.py` (boundary + WDPA gpkg files, Pearson r), plus a hand-reimplementation of the Fisher's z-transformation 95% CI (embedded inline in `research_paper_figures.py`, not in `governance_correlation_test.py` itself).
- **Everything matched exactly**: mangrove areas (Maldives 0.97 flat, Seychelles 3.83–3.84, Fiji 485.72→488.41 km², +0.6%); coral DHW period-comparison changes (+0.17/+0.68/+0.10/+0.08/−0.05) and Mann-Kendall p-values (0.011/0.025/0.184/0.386/0.641, matching which islands are "significant" vs "no trend"); compound vulnerability scores (0.880/0.651/0.467/0.217/0.000); WDPA coastal ratios (3.90/11.32/0.42/2.32/0.00); governance correlation (r=0.727, p=0.164) and its Fisher's z 95% CI ([-0.43, 0.98], n=5).
- **§4.1 explicitly flagged, not independently re-derived:** settlement-based and population-weighted physical-exposure percentages both depend on multi-gigabyte per-island DEM/slope rasters (up to 3.1GB) in `data/terrain/`, impractical to stage/reprocess this round. Both `slr_exposure_analysis.py` and `population_weighted_exposure.py` were read in full for logic review — no obvious bugs found; the existing `data/slr_exposure_summary.csv` output matches the paper's §4.1 table exactly, but that confirms internal consistency, not independent re-derivation from the raw rasters.
- **1 real inconsistency found:** `Research_Paper.md` (§3.6, §4.4, §4.6), `dashboard/pages/4_Compound_Vulnerability.py`, and `research_paper_figures.py`'s Figure 7 title all claimed Seychelles stays highest-ranked across the **entire** 0–100% weighting-sensitivity sweep. Independently reimplementing the sweep shows Seychelles actually leads only from 0% up to a crossover at **w≈0.745 (74.5% physical-exposure weighting)**; Maldives overtakes beyond that. The actual 50/50 weighting used in the study is well inside the range where Seychelles leads, so the central finding is unaffected — but "entire range" is a real overstatement. Fixed in `Research_Paper.md` (3 spots), `dashboard/pages/4_Compound_Vulnerability.py` (caption text), and `research_paper_figures.py` (title text + added crossover annotation); `outputs/plots/fig7_weighting_sensitivity_curve.png` regenerated from the corrected script. The dashboard's own live "what-if" slider was already correct (it recomputes and warns live) — only the surrounding static prose and the static Figure 7 needed fixing. Full traceback of where the overstatement originated documented in `Devlopment_Log.md`.
- **Citations:** spot-checked 3 of 8 references via web search (Bunting et al. 2022 *Remote Sensing* 14(15) 3657; Heron et al. 2016 *Scientific Reports*; Pike 2026 *Marine Conservation Institute*) — all real and correctly cited. Remaining 5 not individually re-verified this round.

**GPIE — Deep Verify (2026-08-03):**
- Re-ran `causal_inference.py`, `causal_inference_placebo.py`, `causal_inference_final_did.py`, `causal_inference_ndvi.py`, and `causal_inference_event_study.py` directly against the project's own `data/master_dataset.csv` and `data/master_dataset_control.csv` in a Python environment with the same pandas/statsmodels stack, and separately hand-wrote the 4 robustness checks (GDP removal, log-transform, ±6/±12-month treatment-date sensitivity, baseline-pollution heterogeneity split) and the minimum-detectable-effect calculation, since none of those have a standalone script in the repo.
- **Everything matched exactly** except two figures: placebo test (−3.29×10⁻⁶, p=0.004); trend-controlled model (p=0.186); corrected two-group NO₂ model (−1.40×10⁻⁶, p=0.663, CI, R²=0.386, N=1930); corrected two-group NDVI model (−0.0210, p=0.012, CI); event-study (exactly 3/23 quarters significant, correct signs and quarters); GDP-removed check (−4.80×10⁻⁷, p=0.880); log-transform (23/1930=1.2% non-positive rows, 10/23 in Dec 2023, coefficient 0.046/≈4.7%, p=0.669); treatment-date sensitivity (p=0.764/0.357/0.151/0.086); heterogeneity split (13/14 country split, both coefficients and p-values); MDE (28.4% of baseline; observed effect ≈4.4%).
- **1 real inconsistency found:** the paper's two "initial single-cohort model" figures (NO₂ Section 4.1, p=0.026; NDVI Section 4.5, p=0.128) were both computed with **classical (non-clustered) standard errors**, not the cluster-robust-by-country SEs the paper's own Section 3.3 and every other model in the paper explicitly use — traceable to the dashboard's own Methodology page already documenting the corrected model's p-value moving from 0.632 (classical) to 0.663 (cluster-robust) once cluster-robust SEs were adopted as the project standard, while the two earliest, already-superseded single-cohort models were apparently never revisited under that later standard. Recomputed cluster-robust: NO₂ initial model is p=0.041 (still significant, narrative unchanged); **NDVI initial model is p=0.0017, not p=0.128 — it was already significant, not null.** This changes the paper's own accurate narrative: the control-group correction improved the NDVI result's identification (isolating an EU-specific effect from a shared regional trend), it did not newly create significance. Fixed in `Research_Paper.md` (§4.1, §4.5, §5 Discussion), `Project_Journal.md` (Methodology Phase 4, Final Findings #2/#3), and dashboard `5_Causal_Results.py`/`6_Methodology.py`; documented in full in `Devlopment_Log.md`. No underlying data, model code, or headline conclusion changes — this was a standard-error consistency fix, not a data or specification error.
- **Citations:** spot-checked 3 of 13 references via web search (Tong et al. 2025 *npj Clean Air*; Riveros-Gavilanes 2023 *JORIT*; Mathew et al. 2024 *Scientific Reports*) — all real. The remaining 10 were not individually re-verified this round; flagged rather than silently assumed checked.
- **PDFs now stale:** `Research_Paper.pdf`, `Project_Journal.pdf`, and `Devlopment_Log.pdf` are stale relative to the corrected `.md` sources and should be regenerated before relying on the dashboard's PDF download buttons (same outstanding item as GREEN_ALIBI and STOLEN_STRATA).

**GHOST_INFRASTRUCTURE — External AI Review fixes applied (2026-08-02):**
- Ran the actual Local Moran's I / LISA spatial-clustering analysis the project's own Objectives had promised but never executed (all 4 reviews flagged this as the top gap) — 97.1% of low-accessibility nodes fall in significant cold-spot clusters, corroborating the t-test finding via an independent method. New script `spatial_clustering_lisa.py`, new figure `lisa_cluster_map.png`, new Research_Paper.md Section 4.4, new Devlopment_Log.md Module 6B, new dashboard section on "The Finding" + "Methodology & Data" pages.
- Added Cohen's d (0.589) and odds ratios (4.9%/100m historical-site, 3.0%/100m city-center) to address "only p-value reported" critique — independently recomputed from raw data this round, exact match.
- Added a new "Future Work" section (Research_Paper.md Section 8) consolidating: socioeconomic confounders, weighted service index, GWR, multi-city comparison, time-series trend, transportation-network digitization, threshold sensitivity, polygon boundaries.
- Reconciled the railway/transportation-infrastructure aim-vs-execution mismatch across Devlopment_Log.md, Project_Journal.md, Research_Paper.md — now explicitly scoped as deferred to Future Work rather than implied as done.
- Softened "continues to structurally determine" → "continues to structurally predict" in Devlopment_Log.md, Project_Journal.md, README.md, matching Research_Paper.md's existing careful language.
- Added a "Ghost Infrastructure" metaphor-precision paragraph to the Discussion (addresses the "does 'ghost' imply neglect?" critique).
- Added a Policy Implications section (Research_Paper.md Section 6).
- Added Görmar & Harfst (2019) reference — verified real, addresses "thin literature review" critique.
- Added honest scope-clarification language in Limitations: historical-mine-count comparison (13 major sites vs. ~550+ broader unverifiable register — no coverage % claimed), and city-center/Rathaus geographic-coincidence note (a genuinely distinct second reference point could not be verified).
- Added a real title to Research_Paper.md (was previously just "# Sakshi D. Maske" with no title).
- **Explicitly skipped** (would require new data/heavy compute not available this round): socioeconomic confounders, GWR, multi-city comparison, time-series trend, actual transportation-network digitization, multiple walking-threshold sensitivity runs, polygon-based boundaries — all captured in Future Work instead of attempted.

**GREEN_ALIBI — Comprehensive review + Deep Verify (2026-08-02):**
- **Major bug found via raw-data audit:** `src/analysis/aggregate_sif.py` globbed for `GOSIF_*.tif` and matched filenames with regex `r"GOSIF_(\d{4})(\d{3})\.tif"` — a pattern that silently matched only the STALE, pre-boundary-fix rectangular-clip files left in `data/processed/clipped/`, and skipped every correctly polygon-masked `_clipped.tif` file (the regex required `.tif` to immediately follow the date, which `_clipped.tif` never satisfies). This meant the project's headline SIF-NDVI lag numbers were quietly computed from the leaky-rectangle boundary the whole time, even though Development_Log.md Entries 4–5 documented believing the corrected boundary had been re-run through "the full chain." The district-level analysis (spatial maps, Pearson/Spearman correlation) was unaffected, since that script hardcoded the correct `_clipped.tif` filename directly.
- Fixed the glob/regex in `aggregate_sif.py`; regenerated `marathwada_sif_timeseries.csv`, `marathwada_sif_ndvi_merged.csv`, `sif_ndvi_lag_by_threshold.csv`, `final_summary_rainfall_lag.csv`, and the two dependent figures, using all 81 raw-boundary-corrected SIF rasters staged from the device.
- **Numbers changed, conclusion did not:** mean lag 24.2→24.3 (2015), 5.1→6.9 (2018), 25.5→28.7 (2020) days; drought-year avg 14.6→15.6, normal year 25.5→28.7. SIF still leads NDVI in all 15 threshold-year combinations; H3 (drought amplifies the lag) is still not supported — if anything slightly more clearly so, since the normal year's lag grew relative to both drought years.
- Updated all six places the stale numbers appeared: `Research_Paper.md` (abstract, §4.2, §5 discussion), `Project_Journal.md` (§7), `README.md`, and dashboard pages 6 (metrics + full per-threshold table) and 10.
- Added `Development_Log.md` Entry 11 documenting the bug, root cause, and fix transparently — matching this project's own established "no compromises" honesty standard rather than silently patching it.
- Independently re-verified from raw/processed data (all matched, no other bugs found): 20-year rainfall climatology mean (826.4mm) and std (158.8mm, sample/ddof=1) and all three years' anomalies; district-level rainfall anomalies for Aurangabad/Bid/Nanded/Hingoli; Pearson r=0.837 / Spearman ρ=0.857 SIF-rainfall correlation (recomputed from the 24 district-year merged CSV, matches to 3 decimals); the full lag-threshold-crossing algorithm (independently reimplemented and reproduced the (pre-fix) reported CSV exactly, confirming the algorithm itself was always correct — only the aggregation-stage bug was wrong).
- Verified all 6 references (Didan 2021 MOD13Q1, FAO GAUL 2015 L2, Friedl & Sulla-Menashe 2022 MCD12Q1, Funk et al. 2015 CHIRPS/Scientific Data, Li & Xiao 2019 GOSIF, PMFBY) against real external sources — all check out, correctly cited.
- Fixed a stale Conclusion sentence in `Research_Paper.md` still calling the SIF-rainfall spatial check "visual (rather than statistical)" after it had already been upgraded to a formal Pearson/Spearman test in an earlier round (Entry 8) — Limitations §6 had been updated but the Conclusion wasn't.
- Populated `requirements.txt` (was a genuine 0-byte file) with the actual imports used project-wide (streamlit, pandas, numpy, matplotlib, rasterio, geopandas, scipy, folium, branca).
- Cleaned up `.gitignore` — it contained a stray PowerShell heredoc wrapper (`@"..."@ | Out-File ...`) baked into the file content itself, from however it was originally created; now contains just the intended ignore patterns.
- Fixed a stale "Sixty 8-day GOSIF GeoTIFFs (twenty per study year)" claim in `Project_Journal.md` and dashboard page 4 — the actual, current pipeline uses 81 files (27/year) after the observation window was extended in Entry 3; this was leftover text from before that extension.
- Added a new Limitations bullet (Research_Paper.md, Project_Journal.md, dashboard page 10) disclosing that the Earth Engine NDVI/land-cover/rainfall acquisition was run interactively in the GEE Code Editor rather than checked in as a script — `src/acquisition/` is empty by design, not by omission, and this is now stated rather than left silently unexplained.
- **Flagged, not fixed (cosmetic, needs the user's own device access):** two unreferenced stray image files in `outputs/figures/` — `idk.jpeg` and `theme.jpeg` — confirmed via full-codebase grep to not be used by any page or document; safe to delete. `fig.jpeg` (used in dashboard page 1 as the study-area reference map) is real and in use, just poorly named — a rename to something like `study_area_reference_map.jpeg` would improve clarity but isn't a correctness issue. Also, the stale un-suffixed `GOSIF_<date>.tif` files in `data/processed/clipped/` (the root cause enabler of the main bug) are still present alongside the correct `_clipped.tif` files — recommend deleting them once the fix is confirmed, since their continued presence is what let the bug hide as long as it did.
- **Follow-up round (same day):** verified all 12 figures referenced in Research_Paper.md visually (correct, none broken/missing); added a clickable Index to `Development_Log.md` (same convention as BORDER_OPTICS/GHOST_INFRASTRUCTURE); added a genuinely new analysis — a time-lagged cross-correlation lag estimate, independent of the threshold-crossing method — as a robustness check on H1. New script `src/analysis/cross_correlation_lag.py`, new figure `cross_correlation_lag.png`, new Research_Paper.md §3.6/§4.6, new dashboard section on page 6, 7th Key Finding in Project_Journal.md. Result: confirms SIF-leads-NDVI in all 3 years by a second method (strengthens H1), but does **not** reproduce the same year-ranking as the threshold method — reported honestly as a method-sensitivity finding rather than suppressed. Also fixed two other stale "visual, not statistical" claims found during this pass (README.md, Research_Paper.md Conclusion) left over from when the SIF-rainfall correlation was upgraded from a visual check to a formal Pearson/Spearman test in an earlier round.
- **Doc Hygiene** now ✅ for GREEN_ALIBI (Index added to Development_Log.md; headings otherwise consistent).
- **Not yet done for this project:** CITATION.cff + LICENSE, Zenodo DOI, Screenshots. PDFs (`Research_Paper.pdf`, `Project_Journal.pdf`, `Development_Log.pdf`) are now stale relative to the corrected `.md` sources and should be regenerated.

**GREEN_ALIBI — External AI Review fixes applied (2026-08-02):**
- Ran GREEN_ALIBI's paper, dev log, and figures past four independent AI reviewers (DeepSeek, Claude, Gemini, ChatGPT). All four gave substantially the same read (methodologically sound, unusually honest limitations) and, more usefully, the same concrete gap list: no uncertainty quantification on the lag estimates, the district-level correlation's non-independence asserted but never measured, the GEE acquisition step still unscripted, and (from one review) RQ3 — the project's own original research question about the official drought-declaration timeline — never actually answered in the paper.
- **Bootstrap confidence intervals** added for the cross-correlation lag estimates. First attempt (moving-block bootstrap on the interpolated daily series) collapsed every year's CI to a degenerate `[0,0]` — block-shuffling a strongly non-stationary decline curve destroys the trend that produces the lag signal. Replaced with case-resampling over the original 17 sparse satellite observations per year. Results: 2015, 13d [5,20]; 2018, 4d [0,9]; 2020, 7d [0,32]. Direction robust (100% of replicates non-negative, every year); exact ranking not statistically distinguishable (every pairwise CI overlaps) — a quantified version of the method-sensitivity caveat already in the paper. New script `bootstrap_lag_uncertainty.py`, new figure, new Research_Paper.md §3.7/§4.7.
- **Moran's I spatial-autocorrelation diagnostic** added for the district-level SIF/rainfall correlation (r=0.837), using a Queen-contiguity weights matrix built from the actual FAO GAUL district polygons. All 6 year×variable checks significant (p<0.05, I=0.26–0.55) — quantifies, rather than just asserts, that the effective n is closer to 3 than 24. New script `spatial_autocorrelation.py`, new figure, new Research_Paper.md §3.8/§4.8.
- **RQ3 closed** — compared 2018's SIF/NDVI decline-onset dates against the one well-documented official Maharashtra drought declaration (31 Oct 2018, verified via Zee News/EPW). Both satellite indicators led the official declaration by ~7 weeks (SIF: 53 days, NDVI: 49 days) — far larger than the 3-4 day SIF-vs-NDVI edge that's the paper's central finding. Reframed the policy argument accordingly: "satellite monitoring generally, refined by SIF specifically" rather than SIF's edge alone being the dominant lever. A verifiable 2015 declaration date could not be located and this is stated explicitly rather than guessed. New script `rq3_official_declaration_comparison.py`, new figure, new Research_Paper.md §3.9/§4.9.
- **GEE acquisition step scripted** — `src/acquisition/gee_data_acquisition.py`, a Python (`earthengine-api`/`geemap`) translation of the previously-interactive NDVI/cropland-mask/CHIRPS extraction. Explicitly caveated as unexecuted/unverified (no EE credentials in this environment) — a reproducibility improvement, not a re-verified replacement.
- Updated Research_Paper.md (Abstract, Discussion, Limitations, Conclusion, +3 references), Project_Journal.md (§5.8, 3 new QA entries, 3 new Key Findings, 2 new Limitations), Development_Log.md (new Entry 13, full honest account including the failed first bootstrap attempt), README.md, requirements.txt (+libpysal, +esda), and dashboard pages 6/9/10.
- Built a new one-page `Policy_Brief.md` + `Policy_Brief.pdf` — a non-technical one-pager summarizing the problem, findings, and policy implication, suggested by multiple external reviews as useful application material.
- **Not attempted this round** (would require new data/access not available): TROPOMI SIF cross-validation, crop-yield ground-truth validation, additional study years, a second study region, GWR. These remain good future-work candidates, not silently dropped.
- Added `CITATION.cff` and `LICENSE` (CC BY 4.0), matching GHOST_INFRASTRUCTURE's exact format/convention (same license text, same CITATION.cff structure with authors/repository-code/abstract/keywords). Added a matching "📜 License" section to README.md.
- User pushed to GitHub and created release `v1.0.0` (tag/title/description provided to match CITATION.cff), which auto-archived to Zenodo: **DOI `10.5281/zenodo.21762501`**. Added the DOI badge to README.md (top, under the tagline), `CITATION.cff` (`identifiers`/`doi` fields), and dashboard `app.py` (centered, directly under the hero title) — matching GHOST_INFRASTRUCTURE's exact placement convention in all three places.
- **Not yet done for this project:** Screenshots (GEE + VS Code). PDFs are still stale relative to the corrected `.md` sources — regenerate `Research_Paper.pdf`, `Project_Journal.pdf`, `Development_Log.pdf` before relying on the in-dashboard PDF download buttons.

**STOLEN_STRATA — Comprehensive review + Deep Verify (2026-08-02):**
- **Bug found and fixed:** `src/analysis/01_extract_karewa_terraces.py` hardcoded the *first, rejected* TPI/slope threshold pair (5, 5) from its own dev log's Day 1 entry — which produces only 115 sliver-shaped candidates — instead of the loosened pair (TPI > 3, slope < 8°) actually used to produce every reported result (201 terraces, 3,305.3 ha, and everything downstream). Confirmed by re-running the DEM→TPI/slope→vectorize step from the raw DEM with both threshold pairs: (5,5) → 115 after area filter, (3,8) → 201, matching every processed `.gpkg`. This is a reproducibility break, not a result-correctness bug — every number ever reported in the paper was always computed from the correct 201-terrace set. Fixed the thresholds and added an inline comment recording the calibration history.
- **Deep Verify — everything else checked out exactly, unusually clean:** independently recomputed the four-point bare-earth-fraction trend (1.84/2.62/2.63/8.43%) directly from raw NDVI rasters; total terrace area (3,305.3 ha); net bare-earth conversion (190.3 ha, 5.8%) and its concentration (128.2 ha / 67% within the 25 flagged terraces); saffron count/area (14 terraces, 225.4 ha) directly from the raw Saffron Index raster; all 6 points of the proximity-risk sensitivity curve (21/29/43/71/86/93% — previously only the 2 endpoints were marked confirmed, with 4 interior points flagged "indicative"; all 6 now confirmed exact); and the road-proximity Mann-Whitney result (75.6m vs 133.1m, p=0.0116) against the full 44,622-segment OSM road network. No other bugs found.
- **Pending item closed:** `10_geomorphometrics_and_figures.py`'s compactness/slope analysis had been run but gated behind `GEOMORPHOMETRICS_CONFIRMED = False` in the dashboard ("being finalised"). Independently recomputed from the raw DEM and confirmed exact: degraded terraces are significantly less compact than intact ones (0.138 vs 0.191, p=0.0044) — a shape-based signal independent of the NDVI bare-earth metric. Slope not significant (p=0.1711). Now incorporated as a confirmed supplementary finding into Research_Paper.md (§3.7/§4.6), Project_Journal.md (Phase 6), and 2 dashboard pages, rather than left pending.
- **Citations verified:** all 12 references in Research_Paper.md checked against real external sources (De Terra & Paterson 1939, Dar & Zeeden 2020, Bhat et al. 2016, Weiss 2001, Boeing 2017, Madasa/Orimoloye/Ololade 2021, Mann & Whitney 1947, Engert et al. 2025 PNAS, Rafi & Syed 2023 Mongabay, Deccan Herald 2022, FAO GIAHS 2012, Press Post 2026) — all real, correctly cited.
- **Doc Hygiene:** fixed a heading-level inconsistency in Devlopment_Log.md (one section was H1 instead of H2, another was unstyled bold/italic instead of a heading); rewrote an embedded Hinglish note into paper-appropriate English prose; added a clickable Index (matching GREEN_ALIBI/GHOST_INFRASTRUCTURE/BORDER_OPTICS convention); corrected a "nine-page dashboard" claim (README ×2, Project_Journal ×1) to the actual 8 pages; fixed "Development_Log.md" → "Devlopment_Log.md" in README (×2) to match the actual on-disk filename; fixed "a evidentiary basis" → "an evidentiary basis"; removed a fictional `docs/` folder from README's and the dashboard's repo-structure diagrams; added a genuine root-level `requirements.txt` (geopandas/rasterio/numpy/scipy/osmnx/matplotlib + dashboard's streamlit/plotly/kaleido) so README's literal "pip install -r requirements.txt" instruction actually works — previously only `dashboard/requirements.txt` existed, scoped to the dashboard alone.
- All 12 changed/new files (script fix, root requirements.txt, README, Project_Journal, Research_Paper, Devlopment_Log, dashboard/data.py, dashboard/app.py, 4 dashboard pages) delivered and written back to the user's device.

**STOLEN_STRATA — External AI Review fixes applied (2026-08-03):**
- User ran 4 independent AI reviews (DeepSeek, Claude, Gemini, ChatGPT personas, framed as Erasmus GEM/CDE panelists). All four converged on the same core critiques: threshold arbitrariness (TPI/slope, 15pp degradation, 0.15 saffron — no sensitivity check reported), the Landsat/Sentinel-2 resolution mismatch only vaguely acknowledged, no effect sizes or multiple-comparison correction on the 3 Mann-Whitney tests, and hard pushback on the 93% saffron-detection shortfall potentially undermining the 43% proximity-risk finding.
- **Threshold sensitivity — new `src/analysis/11_threshold_sensitivity.py`:** swept the degradation threshold 5-30pp (reported 25-terrace count sits in a stable 23-31 range across 12-20pp), the saffron threshold 0.05-0.25 (43% proximity-risk share stable at 39-44% across 0.05-0.175, only destabilising below n=6), and a 3x3 TPI/slope grid (smooth, monotonic, 144-279 candidates, chosen (3,8) pair central).
- **Resolution-mismatch quantified — new `src/analysis/12_robustness_and_effect_sizes.py`:** resampled 2025 Sentinel-2 to 30m (matching earlier years' Landsat) and reran the pipeline — mean bare-earth fraction 8.43%→7.48%, net conversion 190.3→165.2ha, degraded count 25→23. Real, ~13% effect; the underlying acceleration survives it (7.48% still ~2.8x the flat 2005/2015 baseline).
- **Effect sizes + Holm-Bonferroni (same script):** rank-biserial r for all 3 MWU tests (road proximity r=0.268, compactness r=0.352, slope r=−0.170); road proximity and compactness both survive Holm-Bonferroni correction across the 3-test family.
- **Real documentation bug found via the reviews' centroid-distance critique:** Research_Paper.md, Project_Journal.md, and the dashboard's Governance page all described road-distance methodology as "terrace centroid to road" — but `09_road_proximity.py` has always used the full polygon (`geometry.distance(roads_union)`), never a centroid. The paper was wrong, not the code. Fixed the description in all 3 places; this also fully answers the reviews' actual concern since edge-distance was correct all along.
- **Saffron shortfall reframed** (not just re-caveated): added the logical argument that a higher-recall detector would plausibly also catch smaller, less distinct saffron parcels sitting nearer already-mined margins, making 43% a plausible conservative floor rather than an inflated estimate — combined with the threshold-sensitivity result showing 43% is stable across detection settings.
- **RQ4 investigated, not fabricated:** DeepSeek's review suggested a district-level PM Saffron Mission overlay table but used illustrative/invented numbers in its own example. Ran an actual search for real spatially-resolved allocation data — found none beyond the aggregate valley-wide figures already cited. Declined to build a table from invented numbers; RQ4 stays open with the search attempt now documented.
- Added explicit statements of two previously-implicit limitations: no formal accuracy assessment (confusion matrix) exists — only visual QGIS validation — flagged as the top future-work priority; the 2005 bare-earth figure is a 9-year (2001-2009) composite, not a single-year snapshot, since the original single-year query returned zero images.
- Added a causality caveat to the road-proximity finding (correlational, not causal — no dated road-construction record to establish precedence).
- **Explicitly declined this round, with reasons:** a supervised ML saffron classifier (new model-training effort, scoped as separate future work); a least-cost-path road-distance metric (current straight-line polygon-distance is already defensible); a manually-labelled ground-truth accuracy assessment (requires new data collection, not reanalysis); interactive dashboard polish (presentation, not rigor).
- 10 changed/new files (2 new analysis scripts, Research_Paper, Project_Journal, Devlopment_Log, dashboard/data.py, 4 dashboard pages) delivered and written back to the user's device.
- **Not yet done for this project:** CITATION.cff + LICENSE, Zenodo DOI, Screenshots (GEE used interactively + QGIS + VS Code — confirm toolset before that stage).

- Added `CITATION.cff` and `LICENSE` (CC BY 4.0), matching GREEN_ALIBI's/GHOST_INFRASTRUCTURE's exact format/convention (same license text, same CITATION.cff structure with authors/repository-code/abstract/keywords, no DOI fields yet since Zenodo isn't published for this project). Added a matching "📜 License" section to README.md.
- **Not yet done for this project:** Zenodo DOI, Screenshots (GEE + QGIS + VS Code — confirm toolset before that stage).

**Devlopment_Log.md Index — closed out across the portfolio (2026-08-03):** checked all 7 projects; STOLEN_STRATA, BORDER_OPTICS, GREEN_ALIBI, and GHOST_INFRASTRUCTURE already had a clickable Index (added in earlier rounds). Added one, matching the same convention, to the 3 that didn't: DOUBLE_JEOPARDY (originally 19 entries, now 20 after the Deep Verify entry was appended), ECOCIDE (33 entries), and GPIE (originally 49 entries, now 50 after its Deep Verify entry was appended — GPIE's log is unusually large at ~297KB/2900+ lines, so the Sentinel-5P TROPOMI acquisition protocol's ~26 internal subsections are nested as sub-bullets under one top-level entry rather than flattened into the main numbered list). All 3 files delivered and written back to the user's device.

*Last updated: 2026-08-03, after retrofitting the large dashboard DOI badge into all 6 remaining published projects, adding a clickable Devlopment_Log.md Index to the 3 projects that were missing one, and completing Deep Verify for GPIE and DOUBLE_JEOPARDY (2 of the 4 retroactive-plan projects; ECOCIDE, BORDER_OPTICS still to go, one at a time).*
